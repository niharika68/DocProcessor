# Frontend password gate (runtime env var — set in Railway Variables or local .env)
VITE_APP_PASSWORD=your_password_here

AWS_REGION=us-east-1
BEDROCK_MODEL_ID=amazon.nova-pro-v1:0
# Optional: use a full model ARN instead of model ID (takes precedence over BEDROCK_MODEL_ID)
MODEL_ARN=
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
